const PlayerSelect = ({ players, onSelect }) => {
  return (
    <div className="min-h-screen flex items-center justify-center" style={{ color: 'var(--color-text-primary)' }}>
      <div className="text-center">
        <h2 className="text-2xl font-bold mb-4">PlayerSelect</h2>
        <p style={{ color: 'var(--color-text-secondary)' }}>To be implemented in step 04</p>
      </div>
    </div>
  );
};

export default PlayerSelect;
